import { FormatAttributeValuesPipe } from './format-attribute-values.pipe';

describe('FormatAttributeValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAttributeValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
